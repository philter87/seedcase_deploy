name: Continuous Integration

on:
  pull_request:
  push:
    branches: master

jobs:
  Install:
    - uses: actions/setup-python@v3
      with:
        python-version: '3.11'
        cache: 'poetry'
    - run: poetry install